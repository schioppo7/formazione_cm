# Registry credentials with Ansible Vault

`registry_credentials.yml` contains encrypted example variables.

Use `ansible-vault view registry_credentials.yml` or `ansible-vault edit registry_credentials.yml` to open it. Keep the Vault password outside the repository.
