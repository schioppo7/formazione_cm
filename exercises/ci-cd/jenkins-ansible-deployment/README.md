# Jenkins and Ansible deployment

This is a starter exercise for deploying an Nginx application with Jenkins, Docker and Ansible.

Some files are intentionally empty because the pipeline is still incomplete.

The deploy target reads `DEPLOY_TARGET_PASSWORD` from the local environment. The password is not stored in the repository.
