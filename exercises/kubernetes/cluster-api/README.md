# Cluster API with Kind

This exercise creates a Kind management cluster and applies a Cluster API workload manifest.

Use the Docker or Podman Kind configuration from `management-cluster/`. The workload cluster configuration is in `workload-cluster/`.

Generated kubeconfig files are local and ignored by Git.
